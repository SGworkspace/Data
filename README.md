# Student Data Entry System

## 🚀 Quick Deploy

1. **Setup Firebase** (Get config from Firebase Console)
2. **Replace** `firebase-config.js` with your config
3. **Deploy:**
```bash
firebase init hosting
firebase deploy
